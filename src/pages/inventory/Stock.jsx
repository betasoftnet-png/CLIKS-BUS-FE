import React from 'react';
import BusinessStock from '../BusinessStock';

const Stock = (props) => {
    return <BusinessStock {...props} />;
};

export default Stock;
export { Stock, BusinessStock };
